

docker run --rm --network ecom_app_net ghcr.io/tuolib/ecom-patroni:0647a98fb80d \
sh -c "cat /etc/resolv.conf && echo '---' && getent hosts etcd-1"


docker run --rm --network ecom_app_net alpine wget -qO- http://etcd-1:2379/version


subnet
10.40.96.0/20

本项目:
- 使用k3s 部署方式，
- 使用 vpc network, 也就是VIP, 避免宕机
- 使用Nginx 替代 caddy
- 新建了总共5台服务器，
- 通过github action 发布，发布的时候服务器之间不用ssh发布，这样有网络连接问题；
- 然后你帮忙给一个我操作内容最少的方式；
- 然后给我的命令行，一个命令一行显示，方便复制，
- 然后导出为markdown文档


sed 's|https://127.0.0.1:6443|https://45.32.108.143:6443|' /etc/rancher/k3s/k3s.yaml


cat /etc/rancher/k3s/k3s.yaml | sed 's|127.0.0.1|45.32.108.143|' | base64 -w 0

kubectl run dns-debug --rm -it --image=busybox:1.36 --restart=Never -- sh -c '
echo "=== direct 8.8.8.8 ==="
nslookup google.com 8.8.8.8
echo "=== direct 1.1.1.1 ==="
nslookup google.com 1.1.1.1
echo "=== via cluster DNS ==="
nslookup google.com
'

根本原因是 Hairpin NAT：cert-manager pod 运行在 s1/s2 节点，解析 api.find345.site 得到 s3 的公网 IP，然后从集群内部连接公网 IP → 在 Vultr VPS 上这条路径不通（VPS 不支持       
hairpin NAT）。

解决方案：让集群内 DNS 解析 api.find345.site 到 s3 的内网 IP。


S3_IP=$(kubectl get nodes -l role=ingress -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}')
kubectl run curl-test-$(date +%s) -n ecom --rm -it --image=busybox:1.36 --restart=Never -- wget -qO- --timeout=5 "http://${S3_IP}/.well-known/acme-challenge/test" || echo "连接失败"

kubectl run curl-diag-$(date +%s) -n ecom --rm -it --image=curlimages/curl:8.5.0 --restart=Never -- curl -v -L -k --max-time 10 http://api.find345.site/.well-known/acme-challenge/test 2>&1


kubectl run test-curl --rm -it --image=curlimages/curl --restart=Never -- \
curl -s -o /dev/null -w '%{http_code}' http://10.42.0.1/health








