# Redis 7 调优配置

# ── 内存 ──
maxmemory 256mb
maxmemory-policy allkeys-lru

# ── 持久化（RDB） ──
save 900 1
save 300 10
save 60 10000

# ── 网络 ──
tcp-keepalive 300
timeout 0
bind 0.0.0.0

# ── 日志 ──
loglevel notice
