{
  "name": "@repo/cart-service",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "test": "bun --env-file=../../.env test"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "@repo/database": "workspace:*",
    "hono": "^4.7.6",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  }
}
